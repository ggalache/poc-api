spring:
      h2:
        console:
            enabled: true

      datasource:
            url: jdbc:h2:mem:testdb
            driverClassName: org.h2.Driver
            username: sa
            password:
            data: classpath*:db/data/data-h2_*.sql
      jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        #properties:
            #hibernate:
                  #bm2ddl:
                      #importFiles: data-h2_tramos.sql